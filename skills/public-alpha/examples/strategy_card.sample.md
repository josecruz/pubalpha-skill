# Strategy Card — public-alpha-organic-cake

**⛔ NO ENTRY**  ·  confidence **0.53**  ·  balanced / swing/days / lookback 180d  ·  chain `bsc`

> CAKE: calls classified organic (organic score 0.96). Drivers: calls spread out (max 1 in 120 min) → organic timing; varied phrasing across authors (max similarity 0.187).

## The funnel
1. **Narrative heating** — flat _(source: cmc_community_topics+categories)_
2. **Calls** — 5 calls on `CAKE` from seed:podcast:all-in, seed:podcast:forward-guidance, seed:show:tbpn, seed:substack:defi-researcher, seed:x:0xchef
3. **Organic vs coordinated** — **ORGANIC** (organic score 0.96)
   - calls spread out (max 1 in 120 min) → organic timing
   - varied phrasing across authors (max similarity 0.187)
   - 5 distinct authors
   - LLM: substantive thesis (substance 1.0)
4. **On-chain confirmation** — _not available (run with a CMC key)_
5. **Regime gate** — _not available (run with a CMC key)_

## Rules
**Entry (all must hold):**
- `narrative.heating == true`
- `calls.score >= 0.6 AND calls.classification == 'organic'`
- `onchain_confirmation.confirmed == true`
- `regime.state in ['risk_on','neutral']`
- `not_exhausted (price_runup_pct < 50 over 24h)`

**Exit:** SL `-8%` · TP `25%` · time-stop `72h` · invalidations: calls.classification -> coordinated, onchain distribution (buy_sell_ratio < 0.8), attention fades AND price breaks entry

**Sizing:** base `3.0%` (confidence-scaled), max `10%`


_Research artifact — no live execution, no real money. Generated by the Public Alpha CMC Skill._