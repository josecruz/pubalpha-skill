# Strategy Card — public-alpha-coordinated-moon

**⛔ NO ENTRY**  ·  confidence **0.06**  ·  balanced / swing/days / lookback 180d  ·  chain `bsc`

> MOON: calls classified coordinated (organic score 0.238). Drivers: 6 of 6 calls within 120 min → clustered; near-identical phrasing across authors (3-gram Jaccard 1.0) → copypasta. On-chain NOT confirmed (no liquidity/DEX-volume data). Regime risk_off (F&G 20).

## The funnel
1. **Narrative heating** — 🔥 Insurance _(source: cmc_categories+community_trending)_
2. **Calls** — 6 calls on `MOON` from seed:x:alpha_caller7, seed:x:cryptogem_x, seed:x:degenplays, seed:x:moonshot_ky
3. **Organic vs coordinated** — **COORDINATED** (organic score 0.238)
   - 6 of 6 calls within 120 min → clustered
   - near-identical phrasing across authors (3-gram Jaccard 1.0) → copypasta
   - 4 distinct authors, 100% low-follower accounts
   - LLM: low substance — 100x, ape, dont fade, dont miss; little thesis
4. **On-chain confirmation** — ❌ not confirmed — no liquidity/DEX-volume data; buy/sell split not exposed by CMC DEX; holder data not exposed by CMC DEX
5. **Regime gate** — **risk_off** (F&G 20, BTC dom 58.35%, altseason 42)

## Rules
**Entry (all must hold):**
- `narrative.heating == true`
- `calls.score >= 0.6 AND calls.classification == 'organic'`
- `onchain_confirmation.confirmed == true`
- `regime.state in ['risk_on','neutral']`
- `not_exhausted (price_runup_pct < 50 over 24h)`

**Exit:** SL `-8%` · TP `25%` · time-stop `72h` · invalidations: calls.classification -> coordinated, onchain distribution (buy_sell_ratio < 0.8), attention fades AND price breaks entry

**Sizing:** base `3.0%` (confidence-scaled), max `10%`


_Research artifact — no live execution, no real money. Generated by the Public Alpha CMC Skill._