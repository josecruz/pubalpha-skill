"""Public Alpha — CMC strategy Skill helpers.

The funnel: narrative heating -> call extraction -> organic-vs-coordinated
classifier -> on-chain confirmation -> regime gate -> strategy spec -> backtest.
The LLM (the agent) does language work; everything quantitative is here, deterministic.
"""
